import Link from 'next/link';
import {Materia} from "@/app/models/materie";

export default function  nuovamateriaform() {
    return (
       <form action={} className="mx-auto max-w-7xl lg:max-w-7xl mx-auto">
           <div>
               <label htmlFor="Nuovamateria" className="block text-sm font-medium text-gray-900">Nuova materia➕</label>
               <input type="text" id="Nuovamateria" name==inserisci ma
           </div>
       </form>)
}